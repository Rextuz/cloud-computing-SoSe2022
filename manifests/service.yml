apiVersion: v1
kind: Service
metadata:
    name: "cloudcomputing-31dd"
    labels:
        app: "cloudcomputing-31dd"
spec:
    type: LoadBalancer
    ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: http
    selector:
        app: "cloudcomputing-31dd"